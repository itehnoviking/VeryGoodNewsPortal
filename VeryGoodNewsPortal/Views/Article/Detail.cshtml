
@{
    ViewData["Title"] = "Detail";
}

@model ArticleDetailModel

<div class="container">
    <div class="row gx-5">
        <div class="col-3">
            <h1>@Model.Title</h1>
        </div>
        <div class="offset-6 col-3">
            <p>@*@Model.ASourceName*@ Block For Source Name</p>
            <p>@Model.CreationDate</p>
        </div>
        <div class="col-12">
            @Model.Body
        </div>
        <hr />
        <div class="col-12">
            @DateTime.Now.ToString("F")
        </div>
        @*<cache>
            <div class="col-12">
                @await Html.PartialAsync("DateTimePartial")
            </div>
        </cache>*@
    </div>
    <hr />
    <a asp-controller="Article" asp-action="Edit" asp-route-id="@Model.Id">Edit</a>
    <hr />
    <div class="row gx-5">

        Block for Comments
        @*@foreach (var comment in @Model.Comments)
        {
            <div class="offset-2 col-8">
                <div class="card">
                    <h5 class="card-title">@comment.UserName</h5>
                    <div class="card-body">
                        @comment.Text
                    </div>
                    <div class="card-footer text-muted">
                        Published: @comment.CreationDateTime
                    </div>
                </div>
            </div>
        }*@
        <hr />
    </div>
</div>

